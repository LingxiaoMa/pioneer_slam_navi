slam_toolbox:
  ros__parameters:
    solver_plugin: solver_plugins::CeresSolver #lib for solving
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None
    odom_frame: odom
    map_frame: map
    base_frame: base_link #acts as COM
    scan_topic: /scan #incoming data
    use_map_saver: true
    mode: mapping
    debug_logging: false
    throttle_scans: 1 #process every scan not missing any
    transform_publish_period: 0.02
    map_update_interval: 5.0
    resolution: 0.05 #map cell 5cmx5cm
    max_laser_range: 10.0
    minimum_time_interval: 0.5
    transform_timeout: 0.2
    tf_buffer_duration: 30.0
    stack_size_to_use: 40000000
    use_sim_time: true